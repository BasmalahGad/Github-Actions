name: first workflow 
on: 
    push: 
        branches: 
            - main

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: list files
              run: echo "CICD" 

    second_job:
        needs: first_job
        runs-on: ubuntu-latest
        steps:
            - name: list files
              run: echo "CICD 2" 
